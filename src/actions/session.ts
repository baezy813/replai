// Server Actions   
// JD 입력 → Gemini로 질문 생성 → DB 저장
"use server";

export async function createSession(_formData: FormData) {
  // TODO : JD 입력 → Gemini로 질문 생성 → DB 저장
}
